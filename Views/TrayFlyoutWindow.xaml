<Window x:Class="Padlume.TrayFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Padlume"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="320"
        Deactivated="Window_Deactivated"
        SizeChanged="Window_SizeChanged"
        FontFamily="Segoe UI"
        Foreground="{DynamicResource TextPrimaryBrush}">
    <Window.Resources>
        <Style x:Key="FlyoutMenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,9"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="14" Margin="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Opacity="0.5" Color="{DynamicResource ShadowColor}"/>
        </Border.Effect>
        <StackPanel>
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16" Background="{DynamicResource SurfaceBrush}" BorderBrush="#3B82F6" BorderThickness="1.4">
                    <TextBlock Text="&#xE702;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Grid.Column="1" Text="Padlume" FontSize="15" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                <Button Grid.Column="2" Content="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="10" Click="CloseButton_Click"
                        Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextSecondaryBrush}" Width="26" Height="26" Cursor="Hand"/>
            </Grid>

            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="12" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border x:Name="FlyoutAccentBar" Grid.Column="0" CornerRadius="2" Background="{DynamicResource DimBrush}"/>

                    <Border Grid.Column="1" Width="60" Height="60" CornerRadius="12" Background="{DynamicResource SurfaceAltBrush}" Margin="10,0,0,0">
                        <Grid>
                            <Image x:Name="FlyoutPhotoImage" Stretch="Uniform" Margin="6" Visibility="Collapsed"/>
                            <Viewbox x:Name="FlyoutPhotoFallback" Width="30" Height="30" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Path Fill="{DynamicResource TextPrimaryBrush}" Data="M21,6H3C1.9,6,1,6.9,1,8v8c0,1.1,0.9,2,2,2h18c1.1,0,2-0.9,2-2V8C23,6.9,22.1,6,21,6z M11,13H8v3H6v-3H3v-2h3V8h2v3h3V13z M15.5,15c-0.83,0-1.5-0.67-1.5-1.5s0.67-1.5,1.5-1.5s1.5,0.67,1.5,1.5S16.33,15,15.5,15z M19.5,12c-0.83,0-1.5-0.67-1.5-1.5S18.67,9,19.5,9s1.5,0.67,1.5,1.5S20.33,12,19.5,12z"/>
                            </Viewbox>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="FlyoutNameText" Text="{x:Static local:Strings.NoControllerSelected}" FontSize="13" FontWeight="Bold" TextWrapping="Wrap"/>
                        <TextBlock x:Name="FlyoutStatusText" Text="{x:Static local:Strings.NotConnected}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                        <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
                            <Grid Width="40" Height="17">
                                <Border Width="36" Height="17" CornerRadius="4" BorderBrush="{DynamicResource TextPrimaryBrush}" BorderThickness="1.5" HorizontalAlignment="Left"/>
                                <Border Width="3" Height="7" CornerRadius="1" Background="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="3,0,0,0">
                                    <Border x:Name="FlyoutBar1" Width="7.5" Height="11" Margin="0,0,1.5,0" CornerRadius="1" Background="{DynamicResource DimBrush}"/>
                                    <Border x:Name="FlyoutBar2" Width="7.5" Height="11" Margin="0,0,1.5,0" CornerRadius="1" Background="{DynamicResource DimBrush}"/>
                                    <Border x:Name="FlyoutBar3" Width="7.5" Height="11" CornerRadius="1" Background="{DynamicResource DimBrush}"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock x:Name="FlyoutPercentText" Text="--" FontSize="14" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel>
                <Button Style="{StaticResource FlyoutMenuButton}" Click="OpenApp_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8A7;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#8FB4FF" Width="20"/>
                        <TextBlock Text="{x:Static local:Strings.OpenApp}" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource FlyoutMenuButton}" Click="Settings_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#8FB4FF" Width="20"/>
                        <TextBlock Text="{x:Static local:Strings.SettingsLabel}" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>
                <Border BorderThickness="0,1,0,0" BorderBrush="{DynamicResource DividerBrush}" Margin="4,4,4,4"/>
                <Button Style="{StaticResource FlyoutMenuButton}" Click="Exit_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}" Width="20"/>
                        <TextBlock Text="{x:Static local:Strings.Exit}" Margin="6,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
